/*let para = document.querySelector('p');
let nPara = prompt ('Whats your name?!');
if (nPara.length > 0){
para.textContent = 'Welcome '+ nPara;
}

let nColor = prompt ('Enter your favorite color');
if (nColor.length > 0){
para.style.color = nColor;
}

let nSize = prompt ('Enter a size of paragraph');
para.style.fontSize = nSize+'px';
*/

